import React from 'react';
import { ContainerDiv, Conteudo } from './styles';
import  RenderPost from '../../../components/RenderPost';

const ContainerPosts = () => {
    return(
        <ContainerDiv>
           <RenderPost/>
        </ContainerDiv>
    );
}; export default ContainerPosts;
